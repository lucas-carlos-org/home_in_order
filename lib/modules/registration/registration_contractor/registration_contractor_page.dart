import 'package:get/get.dart';
import 'package:flutter/material.dart';
import 'registration_contractor_controller.dart';

class RegistrationContractorPage
    extends GetView<RegistrationContractorController> {
  const RegistrationContractorPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('RegistrationUserPage'),
      ),
      body: Container(),
    );
  }
}
